# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

# User specific aliases and functions
alias alert_grep='grep -i -e warn -e error -e fail -e fatal'
#alias ntpsync='sudo service ntpdate restart'
HISTSIZE=1000000

alias d='pushd'
alias p='popd'
